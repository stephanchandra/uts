<?php
    $host='172.18.0.2';
    $user='root';
    $pass='root';
    $db='Redlock';

    $conn = mysqli_connect($host, $user, $pass, $db);

    if (!$conn){
        echo "Connection Failed!";
        die();
    }

    $query = 

    while (
?>
